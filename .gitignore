# Basic ones
coverage/
/coverage-ts
/node_modules
/.env
/.nyc_output

# We're a library, so please, no lock files
/package-lock.json
/yarn.lock

# Generated types
*.d.ts
*.d.ts.map
!/lib/**/*-types.d.ts
!/index.d.ts
!/packages/*/index.d.ts
!/packages/*/lib/**/*-types.d.ts

# Local upstream-friction tracking (UPSTREAM-tracker) — not committed
/UPSTREAM-*.md

# Local sprint retrospectives (vp-beads retrospective) — not committed
/RETRO-*.md

# Library specific ones
