name: Type Tests, External Types

on:
  push:
    branches:
      - main
    tags:
      - '*'
  pull_request:
    branches:
      - main
  schedule:
    - cron: '14 5 * * 1,3,5'

permissions:
  contents: read

jobs:
  type-tests:
    uses: voxpelli/ghatemplates/.github/workflows/tstyche.yml@main
    with:
      ts-versions: ${{ github.event.schedule && 'next' || '>=5.9,next' }}
      ts-libs: 'es2022;esnext'
